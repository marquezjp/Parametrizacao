version: '3.3'

services:
  db:
    image: mcr.microsoft.com/mssql/server
    container_name: SQLServer
    hostname: dbserver
    restart: always
    environment:
      - MSSQL_SA_PASSWORD=SQLServer2023
      - ACCEPT_EULA=Y
    ports:
      - "1433:1433"
    volumes:
      - P:\DBServer\SEGADDB\Dados:/var/opt/mssql/data
      - P:\DBServer\SEGADDB\Log:/var/opt/mssql/log
      - P:\DBServer\SEGADDB\Secrets:/var/opt/mssql/secrets
      - P:\DBServer\SEGADDB\Dumps:/var/opt/mssql/dumps
